pipeline {
    agent any
    stages {
        stage('Deploy') {
            when { tag "tag-*" }
            steps {
                echo 'Deploying only because this commit is tagged.....'
                echo 'hellow world'
            }
        }
    }
}
